# E8 — wzrok
mss>=9.0                                   # szybkie przechwytywanie ekranu
Pillow>=10.0                               # skalowanie/kodowanie PNG w pamięci
rapidocr-onnxruntime>=1.3                  # S1: OCR na CPU (zamiennik: paddleocr)
numpy>=1.24
PyYAML>=6.0
uiautomation>=2.0 ; sys_platform == "win32"  # S0: drzewo kontrolek Windows
